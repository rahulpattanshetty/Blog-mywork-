 
<h2>Categories</h2>
<% @categories.each do |category|%>
	<ul>
	<li><%= link_to category.name,category_path(category.id)%>(<%= category.articles.length%>) </li>
		
	</ul>
<%end%>
<%if can? :create,Category.new%>
<%= link_to "Add Category",new_category_path%>
<%end%>